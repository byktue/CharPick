name: charpick
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - nodejs
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - pydantic
      - langextract
      - httpx
      - requests
      - tqdm
      - chardet
      - jinja2
      - pandas