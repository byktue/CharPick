# 第一阶段脚本级功能测试和连通测试结果

测试日期：2026-05-09

本阶段目标：先完成脚本级功能测试和连通测试，不启动真实前端页面，先确认后端、前端辅助脚本、Supabase、OSS、远程模型调度都可用。

---

## 1. 后端脚本级测试

执行命令：

```powershell
Set-Location 'E:\codes\CharPick'
E:/code_environment/conda/Scripts/conda.exe run -p E:\code_environment\conda --no-capture-output pytest backend/test/test_llm_dispatcher.py backend/test/test_llm_remote_client.py -q
```

结果：通过，退出码 `0`。

说明：

- `test_llm_dispatcher.py` 通过。
- `test_llm_remote_client.py` 通过。
- 说明后端 LLM 调度与远程 API 适配逻辑正常。

---

## 2. 后端调度脚本测试

执行命令：

```powershell
Set-Location 'E:\codes\CharPick'
E:/code_environment/conda/Scripts/conda.exe run -p E:\code_environment\conda --no-capture-output python test\orchestrator.py
```

结果：通过，退出码 `0`。

说明：

- 学校远程 API 调度路径可用。
- Ollama 备选调度路径可用。

---

## 3. 前端 Supabase 连通测试

执行命令：

```powershell
Set-Location 'E:\codes\CharPick\frontend\frontend_vue'
node .\test_modules\test-supabase-connection.js
```

结果：通过。

输出摘要：

- `users` 表可访问。
- 返回了示例用户数据。

---

## 4. 前端 OSS 连通测试

执行命令：

```powershell
Set-Location 'E:\codes\CharPick\frontend\frontend_vue'
node .\test_modules\test-oss-connection.js
```

结果：通过。

输出摘要：

- 上传成功。
- 下载成功。
- 删除成功。
- 说明 OSS 读写权限和连通性正常。

---

## 5. 前端远程模型连通测试

执行命令：

```powershell
Set-Location 'E:\codes\CharPick\frontend\frontend_vue'
node -e "const fs=require('fs'); const p=require('path'); const cfg=JSON.parse(fs.readFileSync(p.resolve('..','..','backend','config.json'),'utf8')); const key=cfg.llm.remote_api.api_key; require('child_process').spawnSync('node',['.\\test_modules\\test-model-connection.js','--base-url','https://chat.ecnu.edu.cn/open/api/v1/chat/completions','--api-key',key,'--model','ecnu-max'],{stdio:'inherit'});"
```

结果：通过。

输出摘要：

- `models` 接口连通成功。
- `chat.completions` 调用成功。
- 模型返回了正常回复。

说明：

- 这一步曾使用占位令牌导致 `401`，已改为读取 `backend/config.json` 中的真实远程 API Key 后复测通过。

---

## 6. 第一阶段最终结论

第一阶段脚本级功能测试和连通测试已全部通过。

结论：

- 后端脚本测试通过。
- 调度脚本通过。
- Supabase 连通通过。
- OSS 连通通过。
- 远程模型连通通过。

第一阶段不存在未修复的阻塞问题，可以进入第二阶段：启动真实后端服务和前端服务，并进行页面级端到端测试。