database\001_init_auth_l0_l2.sql 这是我目前包含了用户登录管理系统，和L0，L1，L2的数据库结构，我使用的是远程的Supabase服务
docs/工作流搭建 中有相关的文档（包括Supabase服务的相关信息）
backend包含了我之前所做的人设提取工作流
source_files 即为L0层级
chapters 为L1
chapter_extractions 为L2 处理文本统一存储到对应的extraction_json（JSONB）# 章节提取结构化数据中

注意有些是要拉取本地缓存的文件（backend中新建一个配置文件config.json，存储相应的本地缓存文件的地址，Supabase中只存储相应的url）
source_files表中的unified_format_url # 中间结构地址（实际文件存储在本地缓存）
chapters表中的content_text_url # 章节纯文本地址（实际文件存储在本地缓存）
新建一个本地缓存文件夹
	新建一个unified_format文件夹
	新建一个chapter_content_text


注意有些是要存储到远程的oss中，OSS配置查看工作流搭建文件夹中的OSS配置.md即可
extraction_json_url（TEXT）# 章节提取结构化数据 （oss存储）

backend文件夹中
新建一个文件夹，源文件预处理，存储 文本清洗，以及章节逻辑分块版块的文件（将已有的分块版块迁移进去）
	在其中再新建一个文件夹，存储多源文件解析模块文件
新建一个文件夹，存储LLM 逐章结构化提取文件
新建一个test文件夹，存储测试代码（先写，暂不测试，但是在文件夹中也要写一个md操作文件）
main.py文件依旧保留在backend


在backend文件夹中，写一个README

老的处理流程为文本读取 -> 章节切分 -> Prompt + Few-shot 提取 -> 结构化结果写入 JSONL。
位于backend文件夹中

测试数据使用神游.txt（已上传到Supabase中的source_files中）
我当前只有txt版本的数据来源，所以多源文件解析模块先只做txt的
我已完成章节逻辑分块，以及LLM逐章结构化提取，但是逐章结构化提取中，之前考虑到是llm如果一次性串行处理多个维度的信息，而非并行处理的话，llm理解并不是很好，且不严谨，而且需要手动选择提取维度

我需要进行 一些新的改动
整体流程应当为
输入 → 多源文件解析 → 文本清洗 → 章节 / 逻辑分块 → LLM 逐章结构化提取 → 双层数据库存储 → RAG 检索 → 角色 Prompt 生成 → 前端展示
当前我需要你帮我完成的部分是
输入 → 多源文件解析 → 文本清洗 → 章节 / 逻辑分块 → LLM 逐章结构化提取
注意： 多源文件解析，生成统一格式的txt文件，存储在本地缓存的unified_formate，用unified_format_url记录在source_files表中

### 来源（先完成）
- 文本
	- epub，txt，pdf，doc，md
- 图片
	- jpg，gpeg，heic，heif，png，webp 通过OCR提取文字
- 网页爬取

config.json文件中还要包含相应的逐章提取的数据采集prompt，可以参考以下

### L1 -> L2 逐章提取
若无相关信息，则相应部分直接不记录
- **章节信息**
    - 章节号
    - 章节名
    - 故事时间信息
    - 出场角色
- **本章剧情**
    - 章节概要
    - 关键事件

- **本章角色**
    - 姓名、性别、年龄、种族
    - 社会关系（本章出现的关系）
    - 外表描述
    - 本章经历 / 行为
    - 性格表现
    - 人物台词（本章）
- **本章关键物品**
    - 物品名
    - 描述
    - 本章表现
    - 所属角色

- **本章地点 & 背景设定**
    - 出现地点
    - 本章提及的世界观
