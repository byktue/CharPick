哪些版块对接上了远程数据库了？
给我在docs\工作流搭建文件夹中，
chapters表应当存储在本地缓存中，而非上传到相应的远程数据库

给我评价一下，如果我之后使用chapter_extractions表中- extraction_json_url（TEXT）# 章节提取结构化数据 （oss存储） 对应的json文件提取整本书的角色总表、物品总表、剧情时间线、世界观/地点表汇总，我要如何实现？ 角色总表中记录的是出现过的所有角色的清单，按照出场次数进行排序，角色总表中不包含具体的角色卡，但是有status状态记录是否生成角色卡

我更改了config中的"model": "qwen2.5:0.5b", 帮我进行一次测试run_l0_to_l2_pipeline 跑完整链路，告诉我命令行

prompts更名为这个 chapter_prompts，此外，我还需要添加summary_prompts作为后面summary总表提取的prompt（总表是生成本书概览的，包括角色总表，物品总表，剧情时间线，世界观地图表），以及还需要添加card_prompts作为card （展示卡模板）的prompt（展示卡是生成可调用角色卡的）

llm调度程序能否单独分离出来，用作单独版块，对于"chapter_prompts"，"summary_prompts"，"card_prompts"，可以输入不同参数（调度什么ai），输入不同文件进行调度（怎么输入程序）

角色总表调度llm，依照不同维度的prompt，遍历逐章提取的json文件，生成相应提取结果，数据存储到local_cache对应文件夹的summary文件夹，其中分别有summary_json文件夹和summary_md文件夹，其中分别存储相应的json文件，以及用于前端展示的md格式文件

docs文件夹中，写一个新的md文件，给我介绍一下这个调度器（独立的 LLM 调度模块，chapter / summary / card 三条任务走同一套入口）

好的，那么接下来，我们要做相应的card提取的工作了，对于card，是使用者根据summary中提供的人物信息，选取需要制作角色卡的人物，前端交互后输入角色名字传送到后端，然后依照角色名称与prompt进行拼接，遍历逐章提取，提取指定人物的角色卡

