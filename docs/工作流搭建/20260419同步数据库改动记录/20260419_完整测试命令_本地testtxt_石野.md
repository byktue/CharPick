# 20260419 完整测试命令记录

## 1. 目标
- 使用本地 `data/test.txt` 跑完整工作流。
- 包含本地缓存生成、逐章提取、summary、card、远程 Supabase + OSS 写入。
- card 角色名使用「石野」。

## 2. 环境说明
- 直接使用 `E:\code_environment\conda\envs\charpick\python.exe`，避免 `conda activate` 在当前终端里扩展 PATH 失败。
- 工作目录固定为 `E:\codes\CharPick`。

## 3. 可复用命令

### 3.1 语法检查
```powershell
Set-Location E:\codes\CharPick
E:\code_environment\conda\envs\charpick\python.exe -m py_compile `
    backend\remote_persistence.py `
    backend\workflow_runner.py `
    backend\test\test_llm_dispatcher.py
```

### 3.2 完整测试：本地 test.txt + 远程上传 + summary + card
```powershell
Set-Location E:\codes\CharPick
$env:PYTHONIOENCODING = 'utf-8'
@'
import json
from backend.workflow_runner import run_l0_to_l2_pipeline

result = run_l0_to_l2_pipeline(
    source_path='data/test.txt',
    source_type='txt',
    source_file_id='sf_test_20260419_local_test',
    file_name='test.txt',
    filter_noise=True,
    upload_chapters_to_remote=True,
    upload_summary_to_remote=True,
    upload_card_to_remote=True,
    run_summary=True,
    run_card=True,
    card_character_name='石野',
    user_id='u_demo_001',
    username='demo',
    book_id='b_test_20260419_local_test',
    book_title='test',
)

summary = {
    'source_file_row': result.get('source_file_row'),
    'chapter_count': len(result.get('chapter_rows') or []),
    'extraction_count': len(result.get('chapter_extraction_rows') or []),
    'summary_cached': bool(result.get('summary')),
    'card_cached': bool(result.get('card')),
    'remote_persist': result.get('remote_persist'),
    'cache_folder': result.get('cache_folder'),
}

print(json.dumps(summary, ensure_ascii=False, indent=2))
'@ | E:\code_environment\conda\envs\charpick\python.exe -
```

### 3.3 如果 PowerShell 里的中文参数被乱码，改用 Unicode 转义
```powershell
Set-Location E:\codes\CharPick
$env:PYTHONIOENCODING = 'utf-8'
@'
import json
from backend.workflow_runner import run_l0_to_l2_pipeline

result = run_l0_to_l2_pipeline(
    source_path='data/test.txt',
    source_type='txt',
    source_file_id='sf_test_20260419_local_test_shiye',
    file_name='test.txt',
    filter_noise=True,
    upload_chapters_to_remote=True,
    upload_summary_to_remote=True,
    upload_card_to_remote=True,
    run_summary=True,
    run_card=True,
    card_character_name='\u77f3\u91ce',
    user_id='u_demo_001',
    username='demo',
    book_id='b_test_20260419_local_test_shiye',
    book_title='test',
)

summary = {
    'source_file_row': result.get('source_file_row'),
    'chapter_count': len(result.get('chapter_rows') or []),
    'extraction_count': len(result.get('chapter_extraction_rows') or []),
    'summary_cached': bool(result.get('summary')),
    'card_cached': bool(result.get('card')),
    'remote_persist': result.get('remote_persist'),
    'cache_folder': result.get('cache_folder'),
}

print(json.dumps(summary, ensure_ascii=False, indent=2))
'@ | E:\code_environment\conda\envs\charpick\python.exe -
```

## 4. 本次实跑结果
- 本地缓存生成成功。
- chapter_count: 10
- extraction_count: 10
- summary_cached: true
- card_cached: true
- remote_persist 的 task id:
  - chapter_extraction_task_id: 22
  - summary_task_id: 23
  - card_task_id: 24
- summary 上传数量: 4
- card 上传数量: 1

## 5. 结果记录
- 本次完整跑通了本地 `test.txt` 的整条链路。
- 远程上传的章节、summary、card 都成功写入。
- 如果直接在命令行里写中文参数，当前终端可能把 `card_character_name` 显示成 `??`；建议优先使用上面的 Unicode 转义写法，确保 card 角色名稳定是「石野」。

## 6. 本地缓存位置
- `E:\codes\CharPick\local_cache\test_b_test_20260419_local_test`

## 7. 备注
- 当前代码里 `books.status` 已经按照新的语义使用 `done`。
- `source_files.parse_status` 仍然保留 `parsed`，这是源文件解析完成态，不是整本书的完成态。