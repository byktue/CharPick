2026.4.6.
接口协议预设

# 上传处理
- 输入接口
- 处理进度可视化

（上传处理默认生成数据汇总中的信息，并不生成角色库的详细角色卡信息，需要之后根据角色总表，手动选取需要生成的角色）

# 数据汇总
- 角色总表（展示角色名称汇总，连接到角色库）
- 物品汇总
- 剧情概要
- 世界观地图

# 书籍和角色库
不同展示（书，角色）

书籍库，优先展示书籍，点入书籍可查看相关角色
角色库，优先展示角色

# 详细动态角色形象开关
关：生成角色卡时，默认展示全局（整书）形象
开：识别角色形象改变的节点，根据不同章节的形象，生成对应的角色卡


补充：
预览时，默认展示该角色卡全局形象简述，在详细角色页面中，可用查看可选的不同角色卡

